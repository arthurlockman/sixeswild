package controllers.builder;

import view.BuilderApplication;

/**
 *  SetSpawnFrequencyController Class.
 *  Manages the setting of tile spawning frequencies. This will determine
 *  how often tiles of certain types are created while in the Player.
 *
 *  @authors ..., Bryce Kaw-uh
 */
public class SetSpawnFrequencyController
{
    BuilderApplication app;

    /**
     * SetSpawnFrequencyController Constructor.
     * @param app:  the Builder application
     */
    public SetSpawnFrequencyController(BuilderApplication app)
    {
        this.app = app;
    }

    /**
     *  Sets the spawn frequency of a tile.
     */
    public void doAction()
    {

    }
}
