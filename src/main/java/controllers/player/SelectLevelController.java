package controllers.player;

import view.PlayerApplication;

/**
 * SelectLevelController Class.
 * Manages the selecting of levels from the level select menu in Player.
 *
 * @author ..., Bryce Kaw-uh
 */
public class SelectLevelController
{
    PlayerApplication app;

    /**
     * SelectLevelController Constructor.
     * @param app:  the Player application
     */
    public SelectLevelController(PlayerApplication app)
    {
        this.app = app;
    }

    /**
     *  Selects the level indicated by the user and opens the level?
     */
    public void doAction()
    {

    }
}
